include('debug.pika');
include('stdlib.pika');
include('lab/parsing.pika');

ok = ppeg.compileFunction(load('lab/collectJSWords.ppeg'), @collector);
if (!ok) throw("Error compiling the command parser");

src = load($1);
ok = collector(src, @symbols, @i);
if (!ok) throw("Failed parsing at offset " # i);

foreach(@symbols, >print($2));